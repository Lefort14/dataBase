const upload = document.getElementById('btn-upload') as HTMLButtonElement;
const inputUpl = document.getElementById('upload-input') as HTMLInputElement;

upload.addEventListener('click', () => {
    inputUpl.click()
})
